
import React from 'react';

const Vector01 = (props) => {
    const { width, height } = props; 

    return (
      <img src={'vector.svg'} width={width} height={height} />
    );
};

export default Vector01;